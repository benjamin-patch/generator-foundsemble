language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-foundsemble' ]; then cd .. && eval "mv $currentfolder generator-foundsemble" && cd generator-foundsemble; fi

